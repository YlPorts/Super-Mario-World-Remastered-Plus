name: Legacy Android APK workflow (manual only)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    name: Android APK workflow replaced by BUILD 70
    runs-on: ubuntu-latest
    steps:
      - name: Legacy notice
        run: echo "This Android APK workflow was replaced by the PC and Android Godot 4.7.1 BUILD 70 workflow."
